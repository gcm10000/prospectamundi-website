import { Role } from '@/interfaces/Role';

const RoleConstrants = {
    root: 'Root' as Role,
    administrator: 'Administrator' as Role,
    contentCreator: 'ContentCreator' as Role
}

export default RoleConstrants;